# ─── Fichiers à NE PAS publier sur GitHub ───

# Dépendances Node (backend local uniquement)
node_modules/

# Base de données locale des inscriptions (données privées — ne jamais publier)
backend/db.json

# Migrations SQL Supabase (structure interne — ne pas publier sur le site)
*.sql

# Fichiers système
.DS_Store
Thumbs.db
*.log
